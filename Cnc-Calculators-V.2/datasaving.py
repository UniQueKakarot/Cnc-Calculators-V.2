import openpyxl as pyxl


class CuttingSpeedData():
    def __init__(self, cutspd, milld, noft, fpt)
    
    self.cutspd = cutspd
    self.milld = milld
    self.noft = noft
    self.fpt = fpt
    
    def filegen(self):
        pass